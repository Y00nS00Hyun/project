# Backend runtime lock.
#
# Every version below is what the image was running when the corpus embedded
# successfully (captured from the live container with `pip freeze`). It exists
# because `pip install torch` and `>=` ranges in pyproject.toml let the whole
# embedding runtime change on any rebuild -- silently, and with no record of
# what it changed from. An intermittent SIGILL in libtorch_cpu.so is exactly
# the kind of problem that cannot be investigated when the runtime is a moving
# target.
#
# This is a pip CONSTRAINTS file, not a requirements file: it does not decide
# what gets installed -- pyproject.toml's extras still do that -- only which
# version is chosen when something is. Adding a dependency does not require
# touching this file until you want the new package pinned too.
#
# To regenerate after a deliberate upgrade:
#
#   docker compose exec -T backend python -m pip freeze --exclude-editable \
#     | sort > constraints-backend.txt      # then restore this header
#
# torch carries a +cpu local version: it comes from PyTorch's CPU index (see
# Dockerfile.backend), not PyPI, and the CUDA build would add gigabytes this
# deployment never executes.
#
# Captured: 2026-09-14 · python 3.12.14 · linux/amd64

alembic==1.20.0
annotated-doc==0.0.5
annotated-types==0.8.0
anthropic==1.5.0
anyio==4.15.1
argon2-cffi==25.1.0
argon2-cffi-bindings==26.1.0
certifi==2026.7.22
cffi==2.1.1
click==8.5.0
cloudpickle==3.1.2
docstring_parser==0.18.0
document-processing-poc @ file:///app
fastapi==0.141.1
filelock==3.32.3
fsspec==2026.7.0
greenlet==3.5.5
h11==0.16.0
hf-xet==1.6.0
httpcore==1.0.9
httpcore2==2.12.0
httptools==0.8.0
httpx==0.28.1
httpx2==2.12.0
huggingface_hub==1.31.0
idna==3.19
Jinja2==3.1.6
jiter==0.17.0
joblib==1.6.0
lxml==6.1.3
Mako==1.4.1
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdurl==0.1.2
mpmath==1.3.0
narwhals==2.26.0
networkx==3.6.1
numpy==2.5.3
olefile==0.47
packaging==26.3
psycopg==3.3.5
psycopg-binary==3.3.5
pycparser==3.0
pydantic==2.13.5
pydantic_core==2.46.5
Pygments==2.21.0
pypdf==6.18.1
python-docx==1.2.0
python-dotenv==1.2.3
PyYAML==6.0.3
regex==2026.9.10
rich==15.0.0
safetensors==0.8.0
scikit-learn==1.9.1
scipy==1.18.1
sentence-transformers==6.0.1
setuptools==78.1.0
shellingham==1.5.4
sniffio==1.3.1
SQLAlchemy==2.0.52
starlette==1.6.0
sympy==1.14.0
threadpoolctl==3.6.0
tokenizers==0.23.2
torch==2.14.0+cpu
tqdm==4.70.1
transformers==5.17.0
truststore==0.10.4
typer==0.27.2
typing_extensions==4.16.0
typing-inspection==0.4.4
uvicorn==0.52.4
uvloop==0.22.1
watchfiles==1.2.0
websockets==17.1
